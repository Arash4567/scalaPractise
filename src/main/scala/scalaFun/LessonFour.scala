package scalaFun

object LessonFour extends App {
  //  Biz birlamchi konstruktorni nol, bir yoki bir nechta parametr bilan belgilashimiz mumkin.
  class Greeting {}

  /*val DefaultCrustSize = 30
  val DefaultCrustType = "YUPQA"

  // asosiy konstruktor
  class Pizza (var crustSize: Int, var crustType: String) {

    // bir agumentli yordamchi konstruktor
    def this(crustSize: Int) = {
      this(crustSize, DefaultCrustType)
    }
    def this(crustSize: Int, crustType: String) = {
      this(crustSize, crustType)
    }

    // bir agumentli yordamchi konstruktor
    def this(crustType: String) = {
      this(DefaultCrustSize, crustType)
    }

    // nol agumentli yordamchi konstruktor
    def this() = {
      this(DefaultCrustSize, DefaultCrustType)
    }

    override def toString = s"Pitzamizning diameteri $crustSize sm va $crustType hamirli"

  }*/
}
